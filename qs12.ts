//Greetings: Start with the array you used in Exercise 11, 
//but instead of just printing each person’s name,
// print a message to them. The text of each message should be the same, 
//but each message should be personalized with the person’s name
 let fnames:string[]=["mehak","zara","ayesha"];
 let message:string=("would you like to join dance party?");
 console.log(fnames[0] + " " + message);
 console.log(fnames[1] + " " + message) ;
 console.log(fnames[2] + " " + message) ;
 